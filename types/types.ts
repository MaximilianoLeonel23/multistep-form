export interface Plan {
	label: string;
	pricePerMonth: string;
	pricePerYear: string;
	icon: string;
	id: string;
}

export interface AddOn {
	label: string;
	description: string;
	pricePerMonth: string;
	pricePerYear: string;
	id: string;
}
